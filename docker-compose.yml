services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: eleve
      POSTGRES_PASSWORD: eleve
      POSTGRES_DB: eleve
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
